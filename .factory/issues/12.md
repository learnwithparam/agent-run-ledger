---
id: 12
title: The console says nothing useful when the ledger is empty
paths:
  - apps/console/components/empty.tsx
doneWhen: The empty state names what is missing and what will make it appear, and the console suite passes.
---

A new install shows a table with headers and no rows. Nothing tells the reader whether the ledger is
empty or whether it is broken, and those are very different problems.

Say that no runs have been recorded yet, and that one appears the moment the factory claims its first
work item.
